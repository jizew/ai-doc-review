# Core dependencies
streamlit>=1.29.0
python-docx>=1.1.0

# Optional: For track changes mode
docx-revisions==0.1.3
# Alternative: docx-editor (newer library for track changes and comments)
# docx-editor>=0.1.1

# For preserving YAML comments when saving config
ruamel.yaml>=0.17.0

# LLM SDKs
openai>=1.12.0
anthropic>=0.25.0

# Configuration
pyyaml>=6.0.1

# Utilities
tiktoken>=0.7.0
httpx>=0.27.0

# Optional: For better token counting
tokenizers>=0.15.0
